variables:
  ACRNAME:     # Define the name of the Azure Container Registry
  ACRRG:                # Set the resource group for the Azure Container Registry
  AKS_RG:  #AKS Resource Group
  K8S_CLUSTER:  # AKS Cluster Name


